class Graph {
  constructor() {
    this.adjacencyList = {};
  }
  addVertext(vertext) {
    if (!this.adjacencyList[vertext]) {
      this.adjacencyList[vertext] = [];
      return value;
    }
    return false;
  }
}

// Create new Graph
let myGraph = new Graph();
console.log(myGraph);
